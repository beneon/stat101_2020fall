library(ggplot2)
library(dplyr)

#parameter zone
mean = 4.5
sd = 0.5
n = 140

#construct df and plot
df = data.frame(id=seq(1,n),rbc=round(rnorm(n,mean,sd),2))
list_hist = hist(df$rbc,breaks=15,right=FALSE,plot=FALSE)